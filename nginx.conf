server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optional: handle favicon or other static files
    location = /favicon.ico { log_not_found off; access_log off; }
}